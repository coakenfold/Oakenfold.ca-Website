module.exports = function(grunt) {
    // @@ grunt watch-all
    //
    // development-watch-task does the following:
    //
    // NOTE: launches a livereload server for auto refreshes in the browser
    //
    grunt.registerTask('watch-all', [
        'watch:all'
    ]);
};